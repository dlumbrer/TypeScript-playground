function cal (val1: number, val2: number): number{ //YOU must declare the types of the parameters and the return type is optional
    
    //let total: number = val1 + val2 //MUCH more strict
    let total = val1 + val2
    return total;
}

cal(2,2)